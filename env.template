# Django
SECRET_KEY=__CHANGEME__
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
# CSRF_TRUSTED_ORIGINS will be taken from ALLOWED_HOSTS in settings.py
CORS_ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173


# PostgreSQL (для Docker Compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=__CHANGEME__
POSTGRES_DB=django
DB_HOST=db
DB_PORT=5432
# Used by `pg_isready`:
PGUSER=postgres
PGDATABASE=django

#Docker
APP_DOCKER_IMAGE=memory-test:local

# Gateway / ACME (used by docker/gateway nginx.conf via envsubst)
ACME_EMAIL=admin@example.com
ACME_DOMAIN_NAME=example.com