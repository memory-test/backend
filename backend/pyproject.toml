[tool.ruff]
line-length = 79
exclude = [
    ".gitignore",
    "__pycache__",
    ".venv",
    "manage.py",
    "backend/settings.py"
]

[tool.ruff.lint]
select = ["E", "W", "F", "I"]


[tool.ruff.format]
quote-style = "double"

[tool.isort]
profile = "django"
